# Vendored upstream sources — present for development / porting reference only,
# NEVER committed (workspace vendor rule; see upstream_source/README.md).
# GPLv3-compatible upstreams only.
#
# Blender (GPL-2.0-or-later) — the mesh-authoring architecture this crate's
# design derives from. No Blender source is bundled or ported into this crate.
#   git clone --depth 1 https://github.com/blender/blender.git upstream_source/Blender
/upstream_source/Blender/
