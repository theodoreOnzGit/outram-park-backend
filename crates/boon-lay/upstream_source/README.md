# Upstream source

**N/A — original work.** `boon-lay` (Lagrangian decay / TRISO release
simulator: single-particle Monte Carlo, CRP-6 release-fraction cases, TRISO
fuel-particle simulation) has no single upstream repository it forks or
translates. It implements original Lagrangian-decay/TRISO-release physics
from published methodology (e.g. IAEA CRP-6 benchmark cases), not a specific
existing codebase.

If a genuinely reusable upstream reference implementation is identified later
(e.g. a specific open-source TRISO release-fraction code this crate is
modelled on), document it here with the same fields the other crates'
`upstream_source/README.md` files use (repository URL, license, commit,
provenance, licensing note).
