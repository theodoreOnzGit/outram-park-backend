[package]
name = "kovan-literature"
description = "KOVAN nuclear-engineering knowledge archive: PDF → Markdown → KovanDocument → BibTeX."
version = "0.0.0"
edition.workspace = true
license.workspace = true

[dependencies]
kovan-common.workspace = true
serde.workspace = true
toml.workspace = true
pulldown-cmark.workspace = true
# PDF import is still a stub. When implementing, prefer pure-Rust, Android-
# friendly crates (e.g. `lopdf`/`pdf-extract`) and keep everything offline.
