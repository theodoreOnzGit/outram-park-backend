# Paper datasets — extracted, not regenerated

Working datasets for three manuscripts in preparation, assembled **from results
already recorded in this repository**. No case was re-run to build these files.

That constraint is deliberate and it is the point: every number here is
traceable to a committed V&V record that was generated by a named command on a
named date. A figure assembled by re-running a case is a *new* measurement
wearing an old label, and the two diverge silently. Extraction keeps the paper
and the repository the same artifact.

## The three manuscripts in this round

Target set, fixed 2026-09-12:

| # | Directory | Working title | Status of data |
|---|---|---|---|
| 1 | `part2-data-and-transport/` | Part II — nuclear data preparation and MC transport on singly-heterogeneous benchmarks | **complete** — tables extracted |
| 2 | `part3-double-heterogeneity/` | Part III — explicit TRISO vs ring-RPT in an FHR pebble | **complete** — tables extracted |
| 3 | `if97-backward/` | Backward equations where IAPWS-IF97 is silent | source map written; tables to extract |

**The neutronics work is two papers, not one** (maintainer decision,
2026-09-12). Part II establishes the cross sections and the transport kernel
against classical ICSBEP criticals; Part III assumes that and spends its length
on double heterogeneity. A method paper that must first establish its own data,
its own kernel *and* its own geometry treatment is carrying three arguments and
defending none of them well.

**Part II must be posted before Part III** and cited by arXiv number — Part
III's absolute-k bias is explained by Part II's LCT-008 finding.

### Not in this round

| Directory | Status |
|---|---|
| `human-in-the-loop-vv/` | **Deferred.** Source map and extraction plan kept; not targeted in this round. |

Deferred rather than dropped: it needs the most corpus mining of the four, and
it gains from being able to cite Parts II and III — in particular the
cancelling-defects result, which is one of its two strongest exhibits. Bringing
it forward means mining 623 closed beads first (`op-rpb6.2`).

### Suggested sequencing

Three manuscripts in one week is still aggressive — each needs an introduction,
a related-work section, figures, and co-author review. A workable shape:

| | |
|---|---|
| **post first** | Part II — data already extracted, and it is Part III's dependency |
| **post second** | IF97 backward equations — independent of the other two |
| **draft, post after Part II lands** | Part III — needs Part II's arXiv identifier |

They follow
[arXiv:2608.17504](https://arxiv.org/abs/2608.17504) (Part I — Thermal
Hydraulics), whose finding — *with agentic porting, V&V by human expertise
rather than code generation is the bottleneck* — the third manuscript sets out
to characterise rather than restate.

## Rules for anything added here

1. **Cite the record, not the run.** Every CSV row carries the V&V file it came
   from. If a number has no such file, it does not belong in a paper.
2. **Do not hand-edit a number to match a draft.** If a table disagrees with the
   manuscript, the repository is the authority and the manuscript is what
   changes — the reverse is how published figures stop being reproducible.
3. **Regeneration commands belong in the per-paper README**, so a reader can
   re-derive a row without guessing which test produced it.
4. **Preliminary means preliminary.** Every crate in this workspace is still
   marked ❌ on both bookkeeping axes (`README.md`, "Bookkeeping status"), so no
   result here has maintainer V&V sign-off. Part I handled this correctly by
   titling itself "*Initial* Verification and Validation"; keep that framing.
   See `RESPONSIBLE_USE.md` and `VERIFICATION_AND_VALIDATION.md`.

## Acronym

Part I expands OUTRAM PARK as "Multi-**Phase**". From 2026-09-12 the project is
"Multi-**Physics**" — see [`../ecosystem-naming.md`](../ecosystem-naming.md),
which carries the wording for announcing the change in Part II onward. Cite Part
I's title verbatim.
